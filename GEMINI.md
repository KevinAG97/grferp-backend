<!-- # Projeto: Minha Biblioteca TypeScript

## Instruções Gerais
- Use estilo de código já existente.
- Documente todas as funções com JSDoc.
- Prefira programação funcional quando possível.

## Estilo de Código
- Indentação: 2 espaços.
- Interfaces devem começar com `I` (ex: `IUserService`).
- Use `===` e `!==` sempre.

## Arquivo específico: src/api/client.ts
- Responsável por chamadas de API.
- Todas as funções devem ter tratamento de erro robusto.
- Utilize `fetchWithRetry` para requisições GET. -->
